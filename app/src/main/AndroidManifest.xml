<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.nitesh_sunil.fortsinmaharashtra">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".Place_Package.PackageActivity"></activity>
        <activity
            android:name=".Place_Package.PlaceActivity"
            android:theme="@style/AppTheme_ActionBarBlack">
        </activity>
        <activity android:name=".UserClass.MasterActivity" />
        <activity android:name=".WorkWithUs.Selector" />
        <activity android:name=".Login_SignUp.Login_SignUp_Activity" />
        <activity
            android:name=".Dashboard_main"
            android:label=""
            android:theme="@style/AppTheme_NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>